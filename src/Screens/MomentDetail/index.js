import { View, Text } from 'react-native';
import React from 'react';

const MomentDetail = ({ navigation }) => {
	return (
		<View>
			<Text>MomentDetail</Text>
		</View>
	);
};

export default MomentDetail;
